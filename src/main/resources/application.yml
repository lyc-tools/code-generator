server:
  port: 9001

# mysql
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://30.20.105.4:3306/ordercenter?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8
    username: ordercenter
    password: 123qweQWE
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
  resources:
    static-locations: classpath:/static/,classpath:/views/

# Mybatis配置
mybatis:
  mapperLocations: classpath:mapper/**/*.xml

